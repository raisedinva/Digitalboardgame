{
  "name": "digitalboardgame",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "test": "npm run build && node dist/game/tests/basic.test.js",
    "ui:demo": "npm run build && node dist/ui/demo.js"
  }
}
